@{

    var grid = new WebGrid(Model, canSort: true, canPage: true);
    grid.Pager(WebGridPagerModes.All);
    Layout = "~/Views/Shared/_LayoutMaster.cshtml";
}

@*<script src="~/js/filter.js"></script>*@
<br/>
 <div class="container">
      <div class="panel panel-primary">
                <div class="panel-heading"> <label id="lblHeading" class="filter-col control-label labelHead">Province Details</label>  </div>    
            <div class="row" >
                 <div class="col-xs-1">
                           
               </div>
            <div class="col-xs-10">
                           
            <div class="panel panel-primary filterable">
                <div class="panel-heading">
                    <h3 class="panel-title">Province</h3>
                    <div class="pull-right">
                        <button class="btn btn-default btn-xs btn-filter"><span class="glyphicon glyphicon-filter"></span> Filter</button>
                    </div>
                </div>
                <div style=" overflow-x:auto;height:500px" >
                <table class="table">
                    <thead>

                        <tr class="filters">
                              <th width="15px"> <input type="text"  placeholder="Province" disabled> </th>
                            <th width="10px">  <input type="text"  placeholder="Country" disabled> </th>   
                           
                             <th width="15px">  <input type="hidden" disabled></th>

                            @* </div>*@

                        </tr>
                    </thead>
                    <tbody class="table">
                        <tr class="filters">
                            @grid.GetHtml(
                tableStyle: "table",
                headerStyle: "webgrid-header",
                columns: grid.Columns(
                                  grid.Column(columnName: "State", style: "webgrid-fixColumn"),
                
                  grid.Column(columnName: "Country", style: "webgrid-fixColumn"),
   grid.Column(columnName: "Id", style: "webgrid-hide")
                 ))
                        </tr>
                    </tbody>
                </table>
                    </div>
                </div>
</div>
                 <div class="col-xs-1">
                           
               </div>
        </div>
    </div>
     </div>

<script type="text/javascript">
    $(function () {
        debugger;
        $('tbody tr').hover( function(){
            $(this).toggleClass('clickable');
        }).on('click', function(){
            location.href = '/Master/EditProvince/' + $(this).find('td:last').text();
        }); 
    });
</script>  
